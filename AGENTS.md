# AGENTS

## 目的
本文件用于说明在 `Toonflow-web` 仓库内协作开发时的基本约束，帮助代理或开发者快速进入上下文，并在不偏离项目约定的前提下完成任务。

## 仓库定位
- 本仓库是 `Toonflow` 的前端工程。
- 技术栈以 `Vue 3`、`TypeScript`、`Vite`、`Pinia` 为主。
- 后端能力来自 `Toonflow-app`，本仓库默认只处理前端逻辑、界面交互与前端侧数据处理。

## 工作边界
- 优先只修改 `Toonflow-web`，不要联动改 `Toonflow-app`，除非用户明确要求。
- 避免无关重构，优先做最小范围修复。
- 不随意变更 API 协议、请求路径、数据结构命名。
- 保留已有兜底逻辑与兼容逻辑，除非修复目标要求变更。

## 常用目录
- `src/pages`：页面级入口
- `src/views`：主要业务视图与复杂页面组件
- `src/components`：通用组件
- `src/utils`：工具函数与前端侧逻辑
- `src/stores`：Pinia 状态管理
- `scripts`：本地脚本、临时验证脚本
- `docs`：项目文档

## 常用命令
- 安装依赖：`yarn install`
- 本地开发：`yarn dev`
- 生产构建：`yarn build-only`
- 代码检查：`yarn eslint <file>`
- 类型检查：`yarn type-check`

## 开发约定
- 搜索优先使用 `rg`。
- 编辑代码时优先做局部修复，不做大面积格式化。
- 新增验证脚本可放在 `scripts/tmp`，任务完成后是否保留按用户要求处理。
- 提交前至少完成与本次改动直接相关的验证，并保留可复述的结果。

## 前端问题排查偏好
- 涉及上传、解析、切分等行为时，先写最小复现脚本确认输入输出。
- 涉及列表勾选、表格选择、树节点选择时，优先检查唯一键是否稳定。
- 涉及线上环境问题时，优先检查运行时配置、同源路径、Nginx 反代和浏览器持久化状态。

## 提交约定
- 分支命名优先使用语义前缀，如：`fix/...`、`feat/...`、`docs/...`
- commit message 默认使用中文，简明描述实际改动。
- 合并回主开发分支前，先确认当前工作区没有无关文件被误提交。
